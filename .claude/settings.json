{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -X utf8 -c \"import json,sys,subprocess; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path') or d.get('tool_response',{}).get('filePath') or ''; sys.exit(0) if not f.endswith('.py') else sys.exit(subprocess.run([sys.executable,'-m','py_compile',f]).returncode)\"",
            "timeout": 30,
            "statusMessage": "Checking Python syntax..."
          }
        ]
      }
    ]
  }
}
